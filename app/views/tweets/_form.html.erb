
  <div class="card bg-info text-white container text-center">
    <br>
<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<!-- hola -->
  <div class="field">
    <%= form.label :Title %>
    <br>
    <%= form.text_field :Title, class:'col-sm-10'  %>
  </div>
<br>
  <div class="field ">
    <%= form.label :content, class: 'col-sm-1' %>
    <br>
    <%= form.text_area :content, rows:4, cols:97%>
  </div>


<br>
  <div class="actions text-center">
    <%= form.submit 'Submit Tweet', class:'btn btn-primary' %>
  </div>
      <br>
<% end %>
  </div>
